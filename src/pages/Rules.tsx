import {useNavigate} from 'react-router-dom'
import Header from '../components/Header'
import Button from '../components/Button'
const Steps=[['🔐','Посмотри секрет','Каждый игрок по очереди смотрит свою карточку. Мирные видят одно слово, Импостор получает только подсказку.'],['💬','Дай описание','По очереди скажите короткую ассоциацию. Нельзя произносить само слово или слишком очевидно его раскрывать.'],['🕵️','Найди чужого','Обсудите ответы и решите, кто говорит подозрительно. Импостор в это время пытается понять общее слово.'],['🎯','Раскройте ответ','В локальной игре выберите подозреваемого вместе. В онлайн-игре каждый голосует тайно со своего телефона.']]
export default function Rules(){const Navigate=useNavigate();return <main className="shell"><Header/><div className="pageHead"><div><div className="eyebrow">КАК ИГРАТЬ</div><h1>Правила</h1></div><div className="bigSticker">🎭</div></div><section className="rulesGrid">{Steps.map(([Icon,Title,Text],Index)=><article className="infoCard" key={Title}><div className="stepNumber">{Index+1}</div><div className="infoSticker">{Icon}</div><h3>{Title}</h3><p>{Text}</p></article>)}</section><section className="panel tipPanel"><b>Главное правило</b><p>Хорошая подсказка показывает, что вы знаете слово, но не помогает Импостору угадать его мгновенно. «У него четыре колеса» для автомобиля уже подозрительно щедро.</p></section><Button className="secondary" onClick={()=>Navigate(-1)}>Назад</Button></main>}
