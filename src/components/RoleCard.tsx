import {useState} from 'react'
import type {Role} from '../game/types'
import Button from './Button'
export default function RoleCard({Role,Word,Hint}:{Role:Role;Word?:string;Hint?:string|null;Category?:string}){const[Visible,SetVisible]=useState(false);return <div className={`roleCard ${Visible&&Role==='IMPOSTOR'?'danger':''}`}><div className="roleSticker">{Visible?(Role==='IMPOSTOR'?'🕵️':'🧠'):'🔒'}</div><div className="eyebrow">{Visible?'ВАША РОЛЬ':'СЕКРЕТНАЯ КАРТОЧКА'}</div><h2>{Visible?(Role==='IMPOSTOR'?'ВЫ ИМПОСТОР':'МИРНЫЙ ИГРОК'):'Удерживайте кнопку'}</h2><div className={`secret ${Visible?'visible':''}`}>{Visible?(Role==='IMPOSTOR'?Hint||'Вы не знаете загаданное слово':Word):'Роль и слово скрыты'}</div><Button onPointerDown={()=>SetVisible(true)} onPointerUp={()=>SetVisible(false)} onPointerCancel={()=>SetVisible(false)} onPointerLeave={()=>SetVisible(false)}>Удерживайте, чтобы посмотреть</Button><p className="privacyHint">Отпустите кнопку, и секрет снова исчезнет.</p></div>}
